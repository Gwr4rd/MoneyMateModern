package com.codex.moneymate

/**
 * Kotlin entry point. The proven database, backup and synchronization code is
 * inherited while each feature is moved to Kotlin without changing user data.
 */
class MainActivity : MoneyMateActivity()
